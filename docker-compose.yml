services:
  bot:
    image: ghcr.io/sjroesink/chatbuddy:latest
    restart: unless-stopped
    env_file: .env
    volumes:
      - bot-data:/app/data
    environment:
      - DATABASE_PATH=/app/data/bot.db

volumes:
  bot-data:
